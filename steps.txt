1. /init
2. # use comments sparingly. Only comment for complex code.
3.ctrl+V replace the `str_replace_editor` text with a more user friendly message of what this tool call is doing. For example,
  maybe state that a file is being created or edited, along with the name of the file being modified. Also, put this into a
  new component and write tests for it. 
  This is a tough task, so, ultrathink about the best way to implement it.